{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Android",
            "type": "android",
            "request": "launch",
            "appSrcRoot": "${workspaceFolder}/android-app/app/src/main",
            "apkFile": "${workspaceFolder}/android-app/app/build/outputs/apk/debug/app-debug.apk",
            "adbPort": 5037,
            "preLaunchTask": "Build and Run on Device"
        }
    ]
}
